import styled from "styled-components";

export const SectionAnnual = styled.section`
  height: 250px;
  width: 100%;
  background: ${ props => props.theme.colors.backgroundCard};
  border-radius: 0 0 4px 4px;
`